# Required for OpenAI providers (whisper-1, gpt-4o-transcribe, gpt-4o-mini-transcribe,
# gpt-4o-transcribe-diarize, gpt-audio) and for generating synthetic fixtures (tts-1).
OPENAI_API_KEY=

# Optional -- each provider below is skipped automatically if its credential is unset.
XAI_API_KEY=
GEMINI_API_KEY=
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=eastus
